#!/usr/bin/env php
<?php

require_once __DIR__ . '/vendor/autoload.php';

use Symfony\Component\Console\Application;
use Amber\Benchmark\Command\RunCommand;

$app = new Application();
$app->add(new RunCommand());

$app->run();
